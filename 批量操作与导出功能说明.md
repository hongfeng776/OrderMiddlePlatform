# 批量操作与导出功能说明

## 功能概述
本次迭代新增了批量操作权限控制、MinIO文件存储和下载链接生成功能，优化了批量操作体验。

## 后端功能

### 1. MinIO集成
**文件位置**: 
- `order-service/src/main/java/com/orderplatform/order/config/MinIOConfig.java`
- `order-service/src/main/java/com/orderplatform/order/service/MinIOService.java`

**功能**:
- MinIO连接配置
- 自动创建Bucket
- 文件上传功能
- 预签名下载URL生成
- 文件流下载

**配置项** (`application-dev.yml`):
```yaml
minio:
  endpoint: http://localhost:9000
  accessKey: minioadmin
  secretKey: minioadmin
  bucketName: order-exports
  downloadExpiry: 7200
```

### 2. 订单导出服务
**文件位置**: `order-service/src/main/java/com/orderplatform/order/service/OrderExportService.java`

**功能**:
- 生成Excel订单报表
- 保存文件到MinIO对象存储
- 返回文件信息和下载URL
- 导出记录包含：订单号、用户ID、金额、状态、收货人信息等

### 3. 权限控制
**文件位置**:
- `common-core/src/main/java/com/orderplatform/common/annotation/RequiresPermission.java`
- `common-core/src/main/java/com/orderplatform/common/enums/PermissionConstants.java`
- `order-service/src/main/java/com/orderplatform/order/interceptor/PermissionInterceptor.java`
- `order-service/src/main/java/com/orderplatform/order/interceptor/UserInfoInterceptor.java`
- `order-service/src/main/java/com/orderplatform/order/config/WebConfig.java`

**权限定义**:
- `order:admin`: 订单管理权限
- `order:batch:ship`: 批量发货权限
- `order:batch:cancel`: 批量取消权限
- `order:export`: 订单导出权限

**工作流程**:
1. 用户登录时获取角色和权限信息
2. 请求时通过Header传递用户信息
3. 拦截器根据注解检查权限
4. ADMIN角色默认拥有所有权限

### 4. 新增API接口
**导出接口**:
- `POST /order/admin/export`: 导出订单列表到MinIO
- `GET /order/admin/export/download`: 下载导出文件

**权限保护的接口**:
- `/order/admin/list`
- `/order/admin/batch-ship`
- `/order/admin/batch-cancel`
- `/order/admin/batch-logs`
- `/order/admin/batch-details`

### 5. 用户表升级
**新增字段**:
- `role`: 用户角色（USER/ADMIN）
- `permissions`: 权限列表，逗号分隔

## 前端功能

### 1. 订单管理页面
**文件位置**: `frontend/src/views/OrderManagement.vue`

**功能**:
- 管理员专属菜单，仅ADMIN角色可见
- 多条件筛选：订单号、用户ID、状态、时间范围
- 批量发货二次确认弹窗
- 批量取消二次确认弹窗
- 导出进度条显示
- 批量操作日志查看
- 权限不足友好提示

### 2. 请求拦截器
**文件位置**: `frontend/src/utils/request.js`

**功能**:
- 自动添加用户信息到请求Header
- Header字段:
  - `X-User-Id`: 用户ID
  - `X-User-Role`: 用户角色
  - `X-User-Permissions`: 权限列表

### 3. Vuex状态管理
**文件位置**: `frontend/src/store/index.js`

**功能**:
- 用户信息持久化存储
- 支持role和permissions字段
- `isAdmin` getter用于权限判断
- 登录/登出状态管理

### 4. 路由守卫
**文件位置**: `frontend/src/router/index.js`

**功能**:
- 认证检查
- 管理员权限路由保护
- 无权限时跳转首页并提示

## 数据库更新

### SQL脚本
**文件位置**: `sql/init.sql`

**更新内容**:
- 用户表新增role和permissions字段
- admin用户默认角色为ADMIN并拥有全部权限
- 批量操作日志表（batch_operation_log）
- 批量操作详情表（batch_operation_detail）

## 使用说明

### 1. 启动MinIO
```bash
# 下载并启动MinIO
docker run -p 9000:9000 -p 9001:9001 minio/minio server /data --console-address ":9001"

# 浏览器访问 http://localhost:9001
# 默认账号密码: minioadmin/minioadmin
```

### 2. 用户登录
- 管理员账号: admin / 123456
- 普通用户账号: test / 123456

### 3. 批量操作流程
1. 管理员登录后进入"订单管理"页面
2. 勾选需要操作的订单
3. 点击"批量发货"或"批量取消"
4. 填写操作备注（可选）
5. 二次确认后执行操作
6. 查看操作结果统计

### 4. 订单导出流程
1. 在订单管理页面设置筛选条件
2. 点击"导出Excel"按钮
3. 等待导出进度完成
4. 文件自动下载到本地
5. 导出文件存储在MinIO中，路径格式: `exports/yyyy/MM/dd/HHmmssSSS_订单导出.xlsx`

## 注意事项

1. **MinIO必须启动**: 导出功能依赖MinIO，使用前确保MinIO服务正常运行
2. **权限控制**: 所有admin接口都需要权限验证，普通用户无法访问
3. **文件管理**: MinIO中的导出文件可以定期清理，避免占用过多存储空间
4. **二次确认**: 所有批量操作都有二次确认，防止误操作
5. **操作日志**: 所有批量操作都有详细日志记录，可在"批量操作日志"中查看

## 技术栈

- **存储**: MinIO对象存储
- **Excel导出**: Apache POI
- **权限控制**: 自定义注解 + 拦截器
- **前端**: Vue3 + Element Plus
